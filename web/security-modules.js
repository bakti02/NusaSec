(()=>{const root=document.getElementById('security-root');if(!root)return;const cfg={attack:{title:'Know what is exposed.',eyebrow:'ATTACK SURFACE',desc:'Discover assets, external exposure and the context behind each resource.',api:'/v1/product/assets',stats:['Assets','Internet exposure','Risk context','Latest scan'],entity:'assets',empty:'No assets are available from the current NusaSec Core scan.'},threat:{title:'Understand risk relationships.',eyebrow:'RISK & THREAT INTELLIGENCE',desc:'Explore attack paths and the relationships that connect assets to security risk.',api:'/v1/product/attack-paths',stats:['Attack paths','Critical paths','Nodes','Relationships'],entity:'attack paths',empty:'The current Core exposes attack-path intelligence rather than a standalone IOC feed.'},vuln:{title:'Fix what matters first.',eyebrow:'VULNERABILITIES',desc:'Prioritize findings using severity, affected assets and Core risk context.',api:'/v1/product/findings',stats:['Findings','Critical','High','Medium'],entity:'findings',empty:'No findings are available from the current NusaSec Core assessment.'},monitor:{title:'See security changes as they happen.',eyebrow:'MONITORING',desc:'Track configuration changes and telemetry coverage from the latest Core assessments.',api:'/v1/product/changes',stats:['Changes','Added','Removed','Coverage'],entity:'changes',empty:'A second scan is required before configuration changes can be calculated.'},incidents:{title:'Move from detection to response.',eyebrow:'INCIDENT RESPONSE',desc:'Create and transition incidents through the Core reliability workflow. Read-side incident listing is not currently exposed by the supplied Core API.',api:null,stats:['Read API','Create','Transitions','Audit'],entity:'incidents',empty:'Core currently exposes incident creation and status transitions, but no GET /incidents endpoint.'},compliance:{title:'Governance you can prove.',eyebrow:'COMPLIANCE',desc:'Understand regulatory applicability, posture and evidence scope from the Core.',api:null,stats:[],entity:'applicability',empty:''}};const key=new URLSearchParams(location.search).get('view')||'attack';if(key==='monitor'||key==='incidents'||key==='compliance'||key==='entity')return;const c=cfg[key]||cfg.attack;document.getElementById('crumb').textContent=c.eyebrow;const esc=v=>String(v??'').replace(/[&<>"']/g,x=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[x]));const arr=x=>Array.isArray(x)?x:(x?.items||x?.data||x?.results||x?.records||x?.assets||[]);const api=window.NusaSec?.api;function shell(items,state){root.innerHTML=`<section class="security-workspace"><header class="security-head"><div><div class="eyebrow">${c.eyebrow}</div><h1>${c.title}</h1><p>${c.desc}</p></div><div class="security-actions"><button class="security-btn" id="refresh">Refresh</button><button class="security-btn primary" id="focus-action">Investigate</button></div></header><div class="security-toolbar"><input id="q" placeholder="Search ${c.entity}..."/><select id="filter"><option value="">All severity</option><option>CRITICAL</option><option>HIGH</option><option>MEDIUM</option><option>LOW</option></select><span class="data-state">${state}</span></div><div class="security-grid">${c.stats.map((s,i)=>`<div class="security-stat"><span>${s}</span><strong>${i===0&&items.length?items.length:'—'}</strong><small>Core-derived</small></div>`).join('')}</div><div class="security-main"><section class="security-panel"><div class="security-panel-head"><strong>${c.entity[0].toUpperCase()+c.entity.slice(1)}</strong><span>${items.length} loaded</span></div><div class="security-list" id="list">${items.length?items.map((x,i)=>{const name=x.name||x.title||x.external_id||x.domain||x.path_id||x.id||`${c.entity.slice(0,-1)} ${i+1}`;const sev=x.severity||x.risk_score||x.status||x.exposure_type||'';return `<button data-i="${i}"><span class="item-name"><strong>${esc(name)}</strong><small>${esc(x.asset_type||x.provider||x.description||x.rule_id||x.status||'NusaSec Core entity')}</small></span><span class="severity ${esc(String(sev).toLowerCase())}">${esc(typeof sev==='number'?sev:sev||'—')}</span></button>`}).join(''):`<div class="empty-security"><strong>${state==='Core unavailable'?'NusaSec Core is unavailable':'No data available'}</strong><span>${c.empty}</span></div>`}</div></section><aside class="security-panel" id="detail"><div class="side-content"><div class="eyebrow">CONTEXT</div><h3>Select an entity</h3><p>Choose a record to inspect its Core-backed context and available actions.</p></div></aside></div></section>`;items.forEach((x,i)=>document.querySelector(`[data-i="${i}"]`)?.addEventListener('click',()=>select(x)));document.getElementById('q')?.addEventListener('input',e=>{const q=e.target.value.toLowerCase();document.querySelectorAll('#list button').forEach(b=>b.hidden=!b.innerText.toLowerCase().includes(q))});document.getElementById('filter')?.addEventListener('change',e=>{const q=e.target.value;document.querySelectorAll('#list button').forEach(b=>b.hidden=!!q&&!b.innerText.toUpperCase().includes(q))});document.getElementById('refresh')?.addEventListener('click',load)}async function select(x){const d=document.getElementById('detail');let detail=x;if(key==='attack'&&x.id&&api){try{detail=await api(`/v1/product/assets/${encodeURIComponent(x.id)}/dossier`)}catch(e){}}const a=detail.asset||detail;d.innerHTML=`<div class="side-content"><div class="eyebrow">ENTITY DETAIL</div><h3>${esc(a.name||a.title||a.external_id||a.path_id||a.id||'Entity')}</h3><p>${esc(a.description||detail.business_context?.application_name||detail.status||'Core-backed security context')}</p>${[['Severity',a.severity||detail.severity||detail.risk_score||'—'],['Status',a.status||detail.status||'—'],['Type',a.asset_type||a.type||'—'],['Provider',a.provider||'—'],['External ID',a.external_id||'—']].map(r=>`<div class="detail-row"><span>${r[0]}</span><b>${esc(r[1])}</b></div>`).join('')}<div class="context-block"><div class="eyebrow">INVESTIGATION</div><p>Findings: ${detail.findings?.length??'—'} · Related assets: ${detail.related_assets?.length??'—'} · Attack paths: ${detail.attack_paths?.length??'—'}</p><a class="security-btn" href="./security-module.html?view=entity&entity=${encodeURIComponent(a.id||a.external_id||'')}">Open investigation</a></div></div>`}async function load(){if(!api){shell([],'Core unavailable');return}try{const data=await api(c.api);shell(arr(data),'Core connected')}catch(e){shell([],'Core unavailable')}}load()})();
