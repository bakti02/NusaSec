# CMS API Contract

The CMS is designed to communicate with the NusaSec core API over HTTP. Authentication uses the shared session mechanism; content mutations should be auditable and validated against lifecycle state.
